<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>404 - Page Not Found</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background: #f8f9fa;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }
      .error-container {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
      }
      .error-code {
        font-size: 8rem;
        font-weight: bold;
        color: #0d6efd;
      }
      .error-message {
        font-size: 1.5rem;
        margin-bottom: 20px;
        color: #333;
      }
      .btn-custom {
        border-radius: 25px;
        padding: 10px 25px;
        margin-bottom: 1rem;
      }
      .icon {
        font-size: 4rem;
        color: #dc3545;
        margin-bottom: 20px;
      }
      .redirect-message {
        margin-top: 15px;
        color: #6c757d;
        font-size: 0.9rem;
      }
    </style>
  </head>
  <body>
    <div class="container error-container">
      <div class="icon">⚕️</div>
      <div class="error-code">404</div>
      <div class="error-message">
        Oops! The page you’re looking for doesn’t exist.
      </div>
      <p class="text-muted">
        It seems you’re lost in the system. Let’s get you back on track.
      </p>
      <a href="/" class="btn btn-primary btn-custom">🏠 Go to Home</a>
      <a href="/auth/login" class="btn btn-outline-secondary btn-custom"
        >🔑 Login</a
      >
      <p class="redirect-message">
        You will be redirected to Home in
        <span id="countdown">10</span> seconds...
      </p>
    </div>

    <script>
      let timeLeft = 10;
      const countdownEl = document.getElementById("countdown");

      const timer = setInterval(() => {
        timeLeft--;
        countdownEl.textContent = timeLeft;
        if (timeLeft <= 0) {
          clearInterval(timer);
          window.location.href = "/";
        }
      }, 1000);
    </script>
  </body>
</html>
